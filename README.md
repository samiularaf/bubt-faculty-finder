
  # BUBT Faculty Finder App Design

  This is a code bundle for BUBT Faculty Finder App Design. The original project is available at https://www.figma.com/design/2kL6aQRs5suSco98j1PIxI/BUBT-Faculty-Finder-App-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  